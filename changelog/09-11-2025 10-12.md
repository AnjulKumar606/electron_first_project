## Add n8n Changelog Workflow Diagram

- **PR:** #2
- **Author:** AnjulKumar606

This change introduces a Mermaid diagram that visualizes the n8n workflow for automated changelog generation. A minor typo in a console log message within `preload.js` has also been corrected.

### Added

- **mermaid.md**: Adds a new Mermaid flowchart diagram to document the automated changelog generation process using n8n.

### Modified

- **preload.js**: Corrects a minor typo in a console log statement (`loadeded` to `loaded`).

### Impact

None.

### Testing

Verify the Mermaid diagram in `mermaid.md` renders correctly.