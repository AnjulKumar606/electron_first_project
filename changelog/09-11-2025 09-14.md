## Add Workflow Diagram and Fix Typo

-   **Repo**: `AnjulKumar606/electron_first_project`
-   **Author**: <>
-   **PR**: 2
-   **Commit Message**: (no message)

### Summary

This update introduces a Mermaid flowchart diagram to visualize a workflow process. Additionally, a minor typo in a console log message within the `preload.js` script has been corrected.

### Changes

#### Added

-   **`mermaid.md`**: A new file containing a Mermaid diagram that outlines a multi-step process, including webhook triggers, data extraction, and commit processing loops.

    ```diff
    +
    +``` mermaid
    +flowchart TD
    +
    +  %% --- SECTION: Webhook and Metadata ---
    +  subgraph TriggerAndExtract [Webhook & Metadata]
    +    A[Webhook Trigger]
    +    B[Function: Extract metadata]
    +  end
    ```

#### Modified

-   **`preload.js`**: Corrected a typo in a `console.log` statement from "loadeded" to "loaded".

    ```diff
    -console.log('preload: loadeded');
    +console.log('preload: loaded');
    ```

---