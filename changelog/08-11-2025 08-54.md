## Added

### Changes
- **mermaid.md** (Added)
  - A Mermaid diagram was added to visualize the workflow and process related to webhook triggers, metadata extraction, fetching PR commits, processing per-commit details, and aggregating information. The file contains a basic structure for a flowchart.

- **preload.js** (Modified)
  - Added `console.log` statement to log when the preload script is loaded, providing developers with an initial indication of its runtime state.